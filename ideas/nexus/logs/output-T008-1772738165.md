1. Files changed
- Modified: [src/quantlab/data/ingest/ingest_nse_fno_bhavcopy.py](/Users/ranabirbasu/GitHub/quantlab/src/quantlab/data/ingest/ingest_nse_fno_bhavcopy.py)

2. Issues encountered / decisions made
- Found exactly 2 vulnerable extraction points in this file (the two you flagged).
- Added a single helper `safe_extract(zf, member, target_dir)` that:
  - resolves `member` under `target_dir` with `os.path.realpath`
  - verifies the resolved path stays inside `target_dir`
  - raises `ValueError` if it would escape
  - calls `zf.extract(...)` only after validation
- Replaced both direct `z.extract(csv_files[0], worker_dir)` calls with `safe_extract(...)`.
- No other logic was changed.

3. Task completion status
- Complete. Zip-slip protection is now applied to all extraction calls in `ingest_nse_fno_bhavcopy.py`.