**Findings (ordered by severity)**

1. IMPORTANT - Missing option-value guards cause hard crashes (`set -u`)  
   File: [nexus-dispatch.sh:40](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:40), [nexus-board.sh:38](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:38), [nexus-knowledge.sh:79](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-knowledge.sh:79)  
   Problem: flags like `--mode`/`--title`/`--type` read `$2` without checking it exists; script aborts with `unbound variable`.  
   Fix: before each `"$2"` access, assert `[[ $# -ge 2 ]]` and emit a clear error.

2. IMPORTANT - `nexus-knowledge search` has jq code injection via `--type`  
   File: [nexus-knowledge.sh:166](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-knowledge.sh:166)  
   Problem: user input is interpolated into jq program text (`select(.type == "${type}")`). A crafted value can alter query behavior.  
   Fix: pass type via `--arg type "$type"` and use `select(.type == $type)`.

3. IMPORTANT - Priority validation is broken for non-numeric input  
   File: [nexus-board.sh:61](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:61)  
   Problem: `[[ "$priority" -lt 1 ... ]] 2>/dev/null` can fail without user-facing error and exits via `set -e`.  
   Fix: validate with regex first (`[[ "$priority" =~ ^[0-9]+$ ]]`) then numeric bounds.

4. IMPORTANT - ID generation can duplicate task IDs  
   File: [nexus-board.sh:74](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:74), [nexus-knowledge.sh:53](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-knowledge.sh:53)  
   Problem: IDs are derived from line/count, not max existing ID; deletions/concurrency can reuse IDs.  
   Fix: compute max numeric suffix and increment, and/or use UUID/time-based IDs.

5. IMPORTANT - No locking around read-modify-write JSON updates (race/lost updates)  
   File: [nexus-board.sh:97](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:97), [nexus-dispatch.sh:234](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:234), [nexus-knowledge.sh:144](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-knowledge.sh:144)  
   Problem: concurrent runs can overwrite each other.  
   Fix: use lock files (`flock`/portable lock strategy) and atomic temp-write in same directory.

6. IMPORTANT - `parent` JSON construction is unsafe with quotes/backslashes  
   File: [nexus-board.sh:88](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:88)  
   Problem: `printf '"%s"' "$parent"` can produce invalid JSON for special chars.  
   Fix: pass with `--arg parent "$parent"` and set `parent: (if $parent == "" then null else $parent end)`.

7. IMPORTANT - Dispatch changed-files detection can fail in repos without `HEAD`  
   File: [nexus-dispatch.sh:190](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:190)  
   Problem: `git diff --name-only HEAD` fails on fresh repos and aborts script due `set -e`.  
   Fix: guard `HEAD` existence (`git rev-parse --verify HEAD`) and fallback to `[]`.

8. IMPORTANT - Required tools not checked in `dispatch`/`status`  
   File: [nexus-dispatch.sh:81](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:81), [nexus-status.sh:19](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-status.sh:19)  
   Problem: missing `jq`/`codex` causes abrupt failures.  
   Fix: add explicit `command -v jq` and `command -v codex` checks with actionable errors.

9. IMPORTANT - Log/output directories are assumed to exist  
   File: [nexus-dispatch.sh:133](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:133), [nexus-dispatch.sh:224](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:224), [nexus-knowledge.sh:144](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-knowledge.sh:144)  
   Problem: writes fail if `logs/` or `context/` paths are missing.  
   Fix: `mkdir -p` for target dirs before write/append.

10. IMPORTANT - Malformed JSON files crash status/board flows  
    File: [nexus-board.sh:301](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:301), [nexus-status.sh:19](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-status.sh:19), [nexus-status.sh:57](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-status.sh:57)  
    Problem: invalid `tasks.json`/`usage.json`/`dispatches.jsonl` aborts script with raw jq parse errors.  
    Fix: validate with `jq empty`/`jq -e .` first and degrade gracefully with explicit error context.

11. MINOR - `cmd_update` leaks temp file on early return  
    File: [nexus-board.sh:189](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:189), [nexus-board.sh:225](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:225)  
    Problem: `mktemp` happens before “No updates specified.” return.  
    Fix: create temp file only after confirming updates exist.

12. MINOR - `depends`/`tags` CSV parsing keeps whitespace/empties  
    File: [nexus-board.sh:82](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:82), [nexus-knowledge.sh:68](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-knowledge.sh:68)  
    Problem: `a, b,` becomes `["a"," b",""]`.  
    Fix: trim and filter empty strings in jq.

13. MINOR - `--help` UX inconsistency  
    File: [nexus-board.sh:28](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-board.sh:28)  
    Problem: help text says command-specific `--help`, but subcommands don’t implement it.  
    Fix: add per-command help handler or remove that claim.

14. MINOR - `usage()` exits with code 1 even for explicit help  
    File: [nexus-dispatch.sh:35](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:35)  
    Problem: `--help` currently signals failure.  
    Fix: support `usage 0` for help path.

15. MINOR - Non-atomic direct overwrite of `usage.json`  
    File: [nexus-dispatch.sh:258](/Users/ranabirbasu/GitHub/WhatToBuildNext?/nexus/scripts/nexus-dispatch.sh:258)  
    Problem: interruption can leave truncated file.  
    Fix: write to temp file in same dir then `mv`.

---

1. Files changed  
- None by this audit.

2. Issues encountered / decisions  
- `shellcheck` is not installed in this environment, so findings are from manual review plus targeted runtime probes.  
- A pre-existing git diff is present for `nexus/board/tasks.json`; I did not modify repository files as part of this audit.

3. Completion status  
- Full audit of all four requested scripts is complete.